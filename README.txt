Welcome to Joshua Santillo's OoM Machine Learning Classifier! 

please `sudo pip3 install scikit-learn numpy matplotlib scipy itertools` to make sure you have all the required libraries!

set up the configuration with the inputs, outputs, and runs that you need

see which algorithms can be run with `python3 analyze.py -h`
